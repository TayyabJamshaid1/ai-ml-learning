Type hints are added using the colon (:) syntax for variables and the -> syntax for function return types.

We have to define the types of vaiables and functions return type

foods:list=list()
num1:int=9
name:str="Kesa"
def sum(a:int,b:int)->int:
    return a+b
sum(7,9)

-------------------------------------
Advanced Type Hints:

from typing import List,Tuple,Dict,Union
# List of integers
numbers: List[int] = [1, 2, 3, 4, 5]
# Tuple of a string and an integer
person: Tuple[str, int] = ("Alice", 30)
# Dictionary with string keys and integer values
scores: Dict[str, int] = {"Alice": 90, "Bob": 85}

allData:Union[str,int]=90 #Union me ap is ki as a variable k tor pr koi b value string or integer datatype ki save krwa skty hu
print(allData)
allData="Kesa"
print(allData)